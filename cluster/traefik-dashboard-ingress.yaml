apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik-dashboard-ingress
spec:
  rules:
  - http:
      paths:
      - path: /traefik/
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 9000